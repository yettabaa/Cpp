#include <string>
#include <iostream>
#include<iomanip>

#include <sstream>
#include <string>

namespace Zab
{
    int i;
    void puts()
    {
        i = 50;
        std::cout << i << std::endl;
    }
};

int main()
{
    int i;
    // char       buf[17];
    // time_t     now = time(0);
    // struct tm  tstruct;
    // tstruct = *localtime(&now);
    // strftime(buf, sizeof(buf), "%Y%m%d_%H%M%S", &tstruct);

    std::cout << "[" <<buf << "] ";
    std::string s;

    // Zab::puts();
    // puts("pp");

}
